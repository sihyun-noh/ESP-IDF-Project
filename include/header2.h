#ifndef HEADER2_H
#define HEADER2_H

typedef enum { IDLE, SELECTION, PAYMENT, DISPENSE } State;
typedef enum { EVENT_SELECT, EVENT_PAY, EVENT_DISPENSE, EVENT_NONE } Event;

void function2();

#endif // HEADER2_H
